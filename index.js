/**
 * @sayman/sdk — Phase 9
 *
 * Install in any external project:
 *   npm install @sayman/sdk
 *   — OR —
 *   npm install /path/to/sayman-chain/sdk
 *
 * Usage:
 *   import { SaymanClient } from '@sayman/sdk';
 */

export { default as SaymanClient, SaymanClient } from './client.js';